pub mod places
{
    pub mod router;
    pub mod domain;
    pub mod use_cases;
    pub mod irepository;

    mod controller;
    mod domain_to_router;
    mod router_to_domain;
}

pub mod users
{
    pub mod router;
    pub mod domain;
    pub mod use_cases;
    pub mod irepository;

    mod controller;
    mod domain_to_router;
}

pub mod routes
{
    pub mod router;
    pub mod domain;
    pub mod use_cases;
    pub mod irepository;

    mod controller;
    mod domain_to_router;
    mod router_to_domain;
}